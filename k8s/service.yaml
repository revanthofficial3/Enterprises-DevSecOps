apiVersion: v1
kind: Service

metadata:
  name: enterprise-devsecops-service

spec:
  type: NodePort

  selector:
    app: enterprise-devsecops

  ports:
  - protocol: TCP
    port: 80
    targetPort: 8080
    nodePort: 30080