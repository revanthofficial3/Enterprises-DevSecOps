[defaults]
inventory = ./inventory
remote_user = ubuntu
host_key_checking = False
interpreter_python = auto_silent
timeout = 30

[privilege_escalation]
become = True
become_method = sudo
become_user = root
become_ask_pass = False
